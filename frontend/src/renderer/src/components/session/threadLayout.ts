// Width is driven by the --thread-max custom property (default 720px in
// globals.css; the Appearance "wide layout" setting overrides it on :root).
export const THREAD_COLUMN_CLASS = 'mx-auto max-w-[var(--thread-max)] px-10'
