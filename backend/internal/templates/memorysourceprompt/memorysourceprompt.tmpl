Source-memory capture batch. Apply your strict promotion bar: capture only durable, future-relevant facts and ignore transactional, automated, and marketing noise. If nothing meets the bar, leave memory unchanged.

The full contents of each file are inline below — do not search the filesystem.

Source files:
{{- range .Sources }}
- `{{ .Path }}`{{ if .Truncated }} (truncated below; read this path for the full text){{ end }}
{{- end }}

Source contents:
{{- range .Sources }}

### {{ .Path }}

```markdown
{{ .Content }}
```
{{- end }}
