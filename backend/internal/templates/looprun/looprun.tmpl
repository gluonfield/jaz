Scheduled Jaz loop run.

Context:
- Loop: {{ .LoopName }} ({{ .LoopID }})
- Run: {{ .RunID }} scheduled {{ .ScheduledFor }}; now {{ .Now }}
{{- if .MemoryPath }}
- Memory: {{ .MemoryPath }}
{{- end }}
- Previous: {{ .PreviousRun }}

Rules:
- Prior transcripts are not injected; inspect thread IDs explicitly when needed.
{{- if .MemoryPath }}
- If the memory file exists, read it before starting. Keep loop files in that directory; ignore sibling automation dirs.
- Before finishing, create parent dirs as needed and update memory with concise durable Markdown. Omit secrets/full transcripts; report write failures.
{{- end }}
{{ range .Extras }}
{{ . }}
{{ end }}
